---
name: worktree-start
description: >
  main へマージする変更に着手するときの開始手順。関連 Wiki を読み、対応 Issue を確保し、
  origin/main から専用 worktree を切るところまでをまとめて行う。
  「〜を直して」「〜を実装して」「〜を追加して」など、コミットが発生する作業を頼まれたら、
  規模の大小にかかわらず最初にこのスキルを使う。
  既に worktree の中にいる場合や、調査・質問への回答だけでコミットが発生しない場合は使わない。
---

# 作業の着手

> **このファイルはテンプレートです。** `<...>` を実プロジェクトの値に置き換え、
> 採用していない仕組み(Wiki / Issue 連携 / 本番同居 CD)の節は削除してから使う。
> Claude Code で使う場合は `.claude/skills/worktree-start/SKILL.md` に置く。

このリポジトリは**複数のエージェントセッションが同時に走る前提**で運用されている。
着手手順が固定されているのはそのため — メインツリーで直接作業すると、他セッションと
HEAD を奪い合い、自分のコミットが他人のブランチに乗る。

## 手順

### 1. Wiki を読む

コードを grep する前に `wiki/index.md` を読み、関連ページに当たりを付けて読む。
過去の経緯・却下された代替案・踏むと壊れる落とし穴はそこにある。ここを飛ばすと、
一度否定された設計を再提案したり、既知の罠を踏み直したりする。

```bash
cat wiki/index.md
```

**末尾の「未作成ページ」節も読む。** そこは「まだ書かれていないページ」の控えであると同時に、
**他セッションが書くと宣言したページ名の予約台帳**でもある。これから作るページが載っていたら、
その名前に合わせる(同じ概念のページを別名で二重に作らないため)。

### 2. 対応 Issue を確保する

open Issue に同じ話が既にあるかを先に見る。重複起票は履歴を分断するので避ける。

```bash
gh issue list --state open --limit 30
```

**既にある** → その番号を使う。新しく分かった事実があればコメントで足す。

**無い** → 起票する。**これから作るものの仕様・受け入れ基準は Issue 本文に書く**
(設計書は作らない。`wiki/` は「今どうなっているか」の記録であって、これから作るものの
仕様書ではないため)。

```bash
gh issue create --label <bug|enhancement|documentation|...> \
  --title "<一文で何をするか>" \
  --body "<背景 / 対象ファイル(path/to/file.py:123 形式) / 受け入れ基準>"
```

秘匿情報(トークン・鍵・Webhook URL・`config.local.*` の中身)は private リポジトリでも
本文に書かない。

### 3. worktree を切る

```bash
git fetch origin
git worktree add ../<repo>-<作業名> -b <branch> origin/main
```

- ブランチ名は `fix/` `feature/` `chore/` `docs/` のいずれかを前置する。
- **ローカル `main` ではなく `origin/main` から切る。** 本番同居 CD を採用している場合、
  ローカル main はデプロイ待ちで `origin/main` より後ろにいるのが正常な状態であり、
  そこから切ると古い土台の上に積んでしまう。
- 置き場所はリポジトリの**外**の兄弟ディレクトリ。リポジトリ内(`.claude/worktrees/` 等)には
  作らない — テストやツールの探索対象に入り込み、削除し忘れると
  `git worktree list` に載らない孤立ディレクトリとして残り続ける。

以降の編集・コミットはすべて `../<repo>-<作業名>` の中で行う。

## 着手後に効いている制約

作業中ずっと守る必要があるもの。詳細は `CLAUDE.md`。

- **メインツリーで `checkout` / `merge` / `pull` しない**(本番同居 CD を採用している場合)。
  HEAD を前進させるのは CD の役目で、先に進めると CD が「更新なし」と判断して
  本番プロセスが古いコードのまま取り残される。
- **`git add` はファイルを明示指定する。** 他セッションの未コミット変更が混ざりうるため、
  `git diff --cached` で確認してからコミットする。
- **スコープ外の問題を見つけたら、その場で `gh issue create` する。** 今の作業は中断しない。
  チャットに書いただけでは記録に残らず失われる。粒度の判断は `CLAUDE.md` の
  「粒度 — 何件に分けるか」に従う。

実装が固まったら `wiki-ingest`、PR 作成以降は `pr-finish` に進む。
