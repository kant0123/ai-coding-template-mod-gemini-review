---
type: component
summary: <index.md に出る一行要約。ページの内容が変わったらここを直して再生成する>
updated: <YYYY-MM-DD>
related: []
---

# <構成要素の名前>

> `components/` のページ雛形。コピーして `kebab-case.md` として使う(連番は付けない)。
> **コードを読めば分かることは書かない。** 書くのは「どこにあるか」「なぜこうなっているか」
> 「何を踏むと壊れるか」。

<一行要約: この構成要素が何を担っているか>

## 責務

<何をする / 何をしないか。境界を明示する>

## 入口となるファイル

| パス | 役割 |
| --- | --- |
| `<path/to/file.py:123 記号名()>` | <この構成要素を読むならここから> |

<!-- 網羅リストにしない。読み始める場所を 2〜5 個だけ挙げる -->

## 依存関係

- 依存している: [[page-a]], [[page-b]]
- 依存されている: [[page-c]]

## 落とし穴

<この構成要素を触るときに踏みやすい罠。実際に起きた事故があれば経緯ごと書く>

<!-- 例:
- `<関数名>` は呼び出し元がトランザクション内にいることを前提にしている。
  単体で呼ぶとコミットされない(PR #45 で 1 度事故)。
-->

## 経緯

<いつ・なぜ現在の形になったか。関連 PR / Issue を挙げる。過去の失敗した試みも残す>
