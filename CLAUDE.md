# Project Instructions

> このファイルは AI エージェント(Claude Code 等)がリポジトリ直下で参照する行動規約です。
> `<...>` のプレースホルダーを実プロジェクトの値に置き換えてから使ってください。
> 「[オプション]」と付いた節は、そのモジュールを採用しない場合まるごと削除してよい部分です。

## 0. プロジェクト概要 [プロジェクトごとに記入]

- プロジェクト名: `<project-name>`
- 技術スタック: `<例: Python 3.12 / Flask / SQLAlchemy / MySQL>`
- 本番環境: `<例: 自宅サーバー Windows / クラウド PaaS / なし(ライブラリ)>`
- フロント変更後の検証方法: `<例: 実装後に必ずブラウザで動作確認する(モバイル幅 375px 相当を含む)>`

## エージェントの応答・作業方針

- **応答は簡潔に。** 前置き・注意書き・重複した要約を削り、本題に字数を使う。説明を求められたときは、
  詳細な解説を明示的に要求されない限り要点のレベルで答える。
- **進捗報告のペース。** 最初のツール呼び出しの前に、これから何をするかを一文で述べる。作業中は
  重要な発見か方針転換があったときだけ短く報告する。完了時は結論(何が起きたか / 何が分かったか)を
  最初の一文に置き、根拠・詳細はその後に書く。
- **成果物ドキュメントの長さはタスクに合わせる。** Wiki ページ・調査報告・Markdown は中身を尽くしつつ、
  水増しの節・重複する要約・定型文で膨らませない。
- **スコープは要求どおりに。** 日常的な判断は自分で下し、解釈次第で成果物が大きく変わるときだけ確認する。
  要求が誤っている・より良い方法があると考える場合は一文で指摘した上で、依頼されたタスクをそのまま完遂する
  (黙って範囲を狭めたり広げたり別物に作り替えたりしない)。
- **サブエージェント委譲は限定的に。** 委譲するのは、真に独立していて並列化できる大規模な作業
  (広範囲の多ファイル調査など)だけ。数回のツール呼び出しで自分で終えられる作業は委譲しない。
  自分の作業の検証・ダブルチェックのためにサブエージェントを使わない。1 体で足りるなら 1 体にし、
  起動数は少なく保つ。
- **一般的な自己検証の指示は書かない。** 「回答前に再確認する」「最終検証ステップを設ける」といった
  指示はモデルの既定動作と重複し、トークンを浪費するだけなので追加しない。
  ただし**本プロジェクトの外部確認は事実確認であり引き続き必須** — 下記「完了の定義」にある
  検証コマンド・ブラウザ確認・CI の成功確認。
- **修正のナレーションは最小限に。** ユーザーのコード・結論・判断が変わる誤りだけを訂正し、簡潔に述べて
  作業を続ける。影響のない言い間違いは黙って直す。

## ナレッジ Wiki (`wiki/`)

このプロジェクトの知識は `wiki/` 配下の相互リンクされた Markdown 群に蓄積する。
実装前に仕様書を書いて実装後は放置する設計書駆動ではなく、**Wiki は常に「今どうなっているか」を
表す生きた成果物**として維持する。書くのはエージェント、読むのは人間。

### 3 層

| 層 | 実体 | 扱い |
| --- | --- | --- |
| 生ソース | コードベース・テスト・PR / Issue・実行ログ・外部ドキュメント | Wiki からは読むだけ。変更は通常の開発ワークフローで行う |
| Wiki | `wiki/` 配下の md | エージェントが全面的に所有し、維持する |
| スキーマ | 本ファイルのこの節 | 運用しながら人間とエージェントで育てる |

**コードが正、Wiki が従。** 乖離を見つけたら Wiki を直す(コード側の不具合なら別 Issue を立てる)。
Wiki の記述に合わせるためにコードを変えない。

### 構成

```
wiki/
  index.md          全ページのカタログ。ページの追加・改名のたびに更新する
  log.md            時系列の追記専用ログ
  overview.md       システム全体像。ここから各ページへ辿れる入口
  components/       モジュール・機能単位のページ(何が・どこに・どう繋がるか)
  concepts/         横断的な仕組みと設計判断(なぜそうなっているか)
  operations/       運用手順・障害対応(どう動かす・壊れたらどうする)
```

- ファイル名は `kebab-case.md`。**連番は付けない**(設計書と違い、ページに順序の意味はない)。
- **新しいページを作ると決めたら、実体を書く前に `index.md` の「未作成ページ」節へ名前を控える。**
  複数セッションが同時に走る場合、控えないと同じ概念のページが別名で二重に作られる
  (別々の worktree が同じ `index.md` のスナップショットを見て、互いの計画を知らないまま進むため)。
  着手時にこの節を読み、他セッションが予約済みの名前と衝突していないか確認する。
- ページ間リンクは `[[page-name]]`(Obsidian 記法)。リンクは惜しまず張る。
  存在しないページへのリンクを残してよい — 「まだ書かれていないページ」の印として機能する。
  ただし**残したリンクは `index.md` の「未作成ページ」節に控える**(控えの無いリンク切れは
  単なる書き損じと区別できないため。`wiki:lint` はこの規約で判定する)。
- **記法そのものを説明するときはバッククォートで囲む。** 地の文に `[[...]]` と書くと
  リンクとして解釈され、リンク切れになる。
- 各ページ先頭に YAML frontmatter を置く。

  ```yaml
  ---
  type: component | concept | operation
  summary: index.md のカタログに出る一行要約
  updated: YYYY-MM-DD
  related: [page-a, page-b]
  ---
  ```

  `summary` が**一行要約の正**。`wiki/index.md` のカタログはここから生成されるので、
  index を手で書き換えない(`node scripts/wiki-lint.js --write-index` で再生成する)。

  `updated` は**最後にそのページを触った日**。`related` は**双方向**に書く
  (A が B を挙げるなら B も A を挙げる)。`wiki/index.md` と `wiki/log.md` は例外で
  frontmatter を付けない。3 種の `type` のどれにも当てはまらないカタログ / ログであり、
  `related` を持つ意味も無いため。

### 何を書き、何を書かないか

**書く — コードを読んでも分からないこと:**

- なぜこの設計なのか。検討して却下した代替案とその理由
- 事故・障害の経緯と再発防止策
- 暗黙の前提、踏むと壊れる落とし穴
- どこに何があるかの入口(`path/to/file.py:123 記号名()` 形式。**記号名が主、行番号は補助** —
  行番号だけだとコードを編集するたびに黙ってずれる。lint が記号名で突き合わせる)
- 外部サービスの癖、本番環境固有の事情

**書かない — コードを読めば分かること:**

- 関数シグネチャや API パラメータの網羅リスト
- クラス構造・ディレクトリツリーの機械的な写し
- コードのコピー(引用ではなくパスで参照する)

この線引きを守らないと Wiki はコードの陳腐化した複製になり、読む価値もトークン効率も失う。

### 3 つの操作

**Ingest — 変更を Wiki に反映する(PR ごと・必須)**

実装が固まったら、**同じ PR に Wiki の更新を同梱する**(別 PR に切り出さない)。
典型的には 1 PR で 1〜5 ページ。**Wiki に影響しないと判断した場合も、PR 本文にその旨を
一行書く**(判断そのものを省略しない)。手順は `wiki-ingest` スキルにある。

**Query — 着手前に Wiki を読む**

コードを grep する前に `wiki/index.md` → 関連ページの順に読む。過去の経緯・落とし穴・
既存の設計判断はそこにある。調査して分かった価値ある知見(原因分析・比較・調査結果)は、
チャットに流したまま消さず Wiki のページとして還元する。

**還元は事前承認済み。** Wiki に残す価値があると判断したら、「残しますか?」と聞かずに
Issue 起票 → worktree → PR → マージまで進めてよい(判断そのものはエージェントが下す)。
確認を挟むと、判断できているのに人間の一言待ちで知見が消えるため。
価値があるかどうかが微妙なときは、聞かずに「残さない」側に倒す。

**Lint — 健康診断**

検査項目は 2 つに分かれる。**エージェントは 1 ページ内で完結する局所ルール(frontmatter を書く、
テンプレートに従う)はよく守るが、Wiki 全体にまたがる大域的な一貫性は自然には保てない。**
壊れるのは常に後者なので、機械で判定できるものは機械に見張らせる。

| 検査 | 担当 |
| --- | --- |
| 壊れた `[[リンク]]`(`index.md` の「未作成ページ」に控えの無いもの) | `scripts/wiki-lint.js` |
| 孤立ページ・`index.md` の掲載漏れ | `scripts/wiki-lint.js` |
| frontmatter の不備・`type` と置き場所の不一致 | `scripts/wiki-lint.js` |
| `updated` と最終コミット日のズレ・`related` の片方向参照 | `scripts/wiki-lint.js` |
| コード参照(`path:line 記号名`)のファイル実在・行のズレ | `scripts/wiki-lint.js` |
| 参照先コードがページより後に変更されている(注意止まり) | `scripts/wiki-lint.js` |
| `index.md` のカタログと各ページの `summary` の食い違い | `scripts/wiki-lint.js` |
| 矛盾するページ | エージェント |
| コードと乖離した記述 | エージェント |
| 言及されるのにページが無い概念 | エージェント |

機械側は `node scripts/wiki-lint.js`(`package.json` があれば `npm run wiki:lint`)。
`index.md` のカタログは生成物なので、ページを足したり `summary` を直したら
`--write-index` を付けて再生成する(忘れると lint が食い違いとして落とす)。
**参照先コードがページより後に変わっている**ことも見るが、こちらは古くなったと断定できないので
注意止まり(CI は落とさない)。
**Wiki を更新したらローカルでも走らせる** — ただし `updated` の判定は git の最終コミット日を
見るため、**コミット前に走らせると古い日付で落ちる**。確認するならコミットしてから。
CI で回すには `.github/workflows-optional/wiki-lint.yml` を `.github/workflows/` にコピーする。

エージェント側は**ユーザーから依頼されたとき**に洗い出し、修正案とともに報告する。
機械側が通っていることが前提なので、まず `wiki-lint` を走らせてから意味の検査に入る。

**矛盾は 2 種類あり、扱いが違う。**

| 種類 | 例 | 扱い |
| --- | --- | --- |
| コード・テスト・ログと突き合わせて**再導出できる**乖離 | Wiki が「既定 15 分」と書いているがコードは 20 分 | Wiki を直す。依頼を待たない(「コードが正、Wiki が従」の延長) |
| ページ間の**散文的な**矛盾 | 2 ページが別々の理由付けを述べていて、どちらが今の判断か解釈が要る | **勝手に統合しない。**両論を残したまま人間に判定を渡す |

後者を自分の判断で片方に寄せると、消えた側の根拠ごと失われる。さらに「矛盾していた」という
判断自体が後のセッションから事実として読まれるため、**推測を記録に昇格させない**。

### Wiki に書くもの / Issue に書くもの

Wiki は**現状の記録**であって、これから作るものの仕様書ではない。

| 内容 | 置き場所 |
| --- | --- |
| これから作る機能の仕様・受け入れ基準 | GitHub Issue の本文 |
| 実装した結果どうなっているか | `wiki/` の該当ページ |
| いつ何をしたか | `wiki/log.md` と PR 履歴 |

未実装の構想を `wiki/` に置かない。実装が入った時点で、現状として Wiki に記述する。

### log.md の形式

各エントリは以下のプレフィックスで始める(`grep "^## \[" wiki/log.md | tail -5` で直近を追える)。

```markdown
## [YYYY-MM-DD] ingest | PR #123 <一行要約>
- 更新: [[page-a]], [[page-b]]
- 新規: [[page-c]]
```

種別は `ingest`(変更の反映)/ `query`(調査結果の還元)/ `lint`(健康診断)。

## Git ワークフロー(worktree ベース)

複数のエージェントセッションが同時に同じリポジトリで作業することを前提にした運用。
1 つの作業ツリーを共有すると HEAD の奪い合いが起き、自分のコミットが他人のブランチに
乗ってしまう事故が起きるため、実装作業は必ず専用の worktree で行う。

**逐次の手順とコマンドはスキルに置いてある。** ここに書くのは、スキルが発火しなかった
場合でも守られている必要がある不変条件だけ。手順を CLAUDE.md に全部書くと、毎回全文が
コンテキストに載るうえ、手順とルールが混ざって「何が絶対で何が段取りか」が読めなくなる。

| 局面 | スキル |
| --- | --- |
| 着手 — Wiki を読む / Issue を確保する / worktree を切る | `worktree-start` |
| 実装後 — 変更を `wiki/` に反映する | `wiki-ingest` |
| 完了 — PR 作成 / CI / マージ / 後始末 / デプロイ確認 | `pr-finish` |
| CI 緑の後・マージ前 — 差分を agy でレビューし、差し戻しを評価する [オプション] | `agy-review` |

スキルの実体は `.agent/skills/<name>/SKILL.md`(Claude Code で使うなら
`.claude/skills/<name>/SKILL.md` に置く)。スキルの仕組みを持たないツールを使う場合は、
各 SKILL.md の中身をこの節の下に展開して書く。

### 常に守ること

- **main へ直接 push・直接マージしない。** 必ず PR を経由し、CI の成功を確認してから
  マージする。マージ方式は `--merge`(squash / rebase は履歴の体裁次第で選ぶ)。
- **実装作業はメインツリーで行わない。**
  `git worktree add ../<repo>-<作業名> -b <branch> origin/main` で作った
  **リポジトリ外の**兄弟ディレクトリで作業する(`.claude/worktrees/` 配下等、
  リポジトリ内には作らない)。ブランチ名は `fix/` / `feature/` / `chore/` / `docs/` を
  前置し、**ローカル `main` ではなく `origin/main` から切る**。
- **`git add` はファイルを明示指定する**(`git add -A` / `git add .` は使わない)。
  他セッションの未コミット変更が作業ツリーに混ざっていることがあるため、
  `git diff --cached` でステージ内容を確認してからコミットする。
- **`wiki/` の更新は同じブランチ・同じ PR に含める。** コミットは分けてよい
  (`docs: wiki を更新`)が、PR は分けない。実装と知識の反映がずれると Wiki は必ず腐る。
- **worktree は使い終わったら消す。** 放置すると `git worktree list` に載らない孤立
  ディレクトリが残り続ける。削除は必ずメインツリーに `cd` してから実行し、失敗しても
  手で `rm -rf` / `Remove-Item` しない(git がその worktree を working tree と認識しなくなり、
  `--force` でも回復しなくなる)。復旧手順は `pr-finish` と `scripts/worktree-cleanup.ps1`。
- **push・PR 作成・マージ・ブランチ削除・worktree 削除はこのルールを事前承認として扱い、
  都度の確認は不要。** ただし `git status` で意図しない変更が混ざっていないか確認してから
  実行すること。CI 成功の確認まで済んだら、マージしてよいかを改めて聞かない。
- **CI が失敗したらマージせず原因を調査する。** 修正が困難なら PR は開いたまま状況を
  報告する(勝手に close しない)。タスクが失敗・中断した場合も勝手にマージ / 削除せず
  状況を報告し、worktree は作業内容を失わないよう残す。

コミットメッセージは `feat:` / `fix:` / `docs:` / `chore:` プレフィックス + 概要。

## [オプション] 本番同居チェックアウトの追加ルール

開発機と本番機が同一マシン・同一ディレクトリで、CD がそのディレクトリの HEAD を
直接前進させて反映する構成(`docs/development_workflow.md` の「同一マシン CD」参照)を
採用している場合のみ、この節を有効にする。採用していなければこの節は削除してよい。

- **メインツリーの HEAD を前進させるのは CD の役目。** エージェントは以下を
  メインツリーで実行しないこと。
  - `git checkout`(作業ブランチへの移動)
  - `git merge`(ローカルマージ)
  - `git pull`(手動での前進)

  先に HEAD を進めてしまうと CD は「更新なし」と判断して何もせず終了し、
  本番プロセスが古いコードのまま取り残される。ローカル main が `origin/main` より
  後ろにいるのは異常ではなく、デプロイ待ちの正常な状態。
  (この状態は CD 側でも検知される — 「更新なし」で終わる前にヘルスチェックの稼働中コミットと
  突き合わせ、不一致なら `drift` としてジョブを失敗させる)
- マージ後の反映確認は `git pull` ではなく、次のいずれかで行う。
  - ヘルスチェックエンドポイントの `commit`(稼働中コミットの short SHA)
  - `deploy` ワークフローの job summary / `::notice::deploy result:` 行に出る `status`
    (`deployed` / `no-op` / `drift` 等。意味は `docs/development_workflow.md`)
  - `logs/deploy-history.jsonl`(git 管理外。デプロイ履歴が 1 行ずつ残る)
- **デプロイスクリプト(`deploy/*.ps1`)を変更したら PR 本文で明示する。** 実行されるのは
  merge 前の(＝ひとつ前の)スクリプトなので**変更は 1 デプロイ遅れて効く**。その回の結果を
  新ロジックのものと読み違えないこと。逆にワークフロー YAML は main から読まれるため即座に
  効く — スクリプトが壊れて CD が止まったら、まずワークフロー側で回避できないか考える。
- **`.ps1` は UTF-8 (BOM 付き) で保存する。** BOM が無いと Windows PowerShell 5.1 が CP932
  として読み、日本語が化けてスクリプトが 1 行も実行されずパースエラーで落ちる。
- **メインツリーを直接編集しない。編集したまま放置しない。** 実装作業は必ず worktree で行う
  (「Git ワークフロー」節のとおり)。この構成では次の連鎖が起きる。

  1. メインツリーに未コミットの変更があると、CD の `git merge --ff-only` が
     `Your local changes ... would be overwritten by merge` で失敗する
  2. 失敗を受けたロールバック(`git reset --hard`)が、**その未コミット変更を破棄する**

  結果として**デプロイが止まり、かつ編集内容が消える**。開発機と本番機が同じディレクトリで
  ある以上、人間がエディタでファイルを開いて直しただけでこの状態になる。
  さらに厄介なことに、エラーは「ローカルが分岐している可能性」と報告されがちで
  **実際の原因(未コミット変更)を示さない**。デプロイスクリプト側で
  `git status --porcelain` を先に見て、変更があればロールバックを走らせずに止める作りにしておく。
- **CI のジョブを増やすときは、デプロイが待っているワークフローに相乗りさせない。**
  `workflow_run` で CI ワークフロー全体の conclusion を待つ構成では、相乗りさせたジョブが
  落ちるとデプロイごと止まる。Wiki の整合性チェックのように**デプロイの可否と無関係な検査は
  別ワークフローに分ける**(`.github/workflows-optional/wiki-lint.yml` はそうしてある)。

## [オプション] GitHub Issue / Projects 自動連携

Issue ベースで作業履歴を残す運用を採用する場合のみ有効にする。

リポジトリ: `<owner>/<repo>`(`gh` CLI 認証済み。Projects を使うなら `project` スコープも)。
Projects ボード: `<https://github.com/users/<owner>/projects/<番号>>`。

**Projects ボードは任意。** 導入していないなら、以下の `gh project ...` の行と
ステータス更新の手順をすべて削除する(Issue の作成・クローズだけでも運用は成立する)。
その場合は「ボード連携が必要になったら本節に追記する」と一行残しておくと、
未導入なのか書き漏れなのかがエージェントから区別できる。

既存ラベル: `<type:feature / type:operation / priority:high / priority:mid / priority:low /
status:blocked / bug など、プロジェクトの実ラベルに置き換える>`。

### 作業に着手するとき

main へマージする変更を伴う作業は、規模にかかわらず着手時に必ず Issue を作る
(調査・原因分析・質問への回答のみでコミットが発生しない作業は対象外)。
worktree を作るのと同じタイミングで行う — 着手の手順は `worktree-start` スキルにある。

1. 対応する open Issue が既にあればそれを使う(重複起票しない)。
2. 無ければ `gh issue create` で作成する(タイトルは一文、本文に背景・対象ファイル)。
   **設計書を別ファイルで作らず、これから作るものの仕様・受け入れ基準は Issue 本文に書く**
   (「ナレッジ Wiki」の役割分担を参照)。
3. (Projects を使う場合)`gh project item-add <番号> --owner <owner> --url <issue の URL>`
   でボードに追加し、ステータスを `In Progress` に更新する。
4. PR 本文に `#<番号>` を含める。

### タスクが完了したとき

1. `gh issue close <番号> --comment "<実装内容の要約>"` でクローズする。
2. (Projects を使う場合)ボードのステータスを `Done` に更新する。

**クローズの成否にかかわらず、実装内容の要約は必ず Issue に残す**(既にクローズ済みなら
`gh issue comment <番号> --body` で補う)。そのため PR 本文・コミットメッセージで
`Fixes #<番号>` / `Closes #<番号>` は**使わない** — マージ時に GitHub が Issue を
自動クローズすると、後段の `gh issue close --comment` が `already closed` で失敗し、
コマンドごと中断して**要約が Issue に残らない**。素の `#<番号>` 参照に留める
(詳細は `pr-finish` スキル)。

### スコープ外の問題を発見したとき

**発見した時点で、その場で起票する。**「あとでまとめて」「報告だけして判断を仰ぐ」はしない
(チャットに書いただけでは記録に残らず、そのまま失われるため)。今の作業は中断せず、
起票してから続きに戻る。

対象 — 今の作業では直さないと決めたもの:

- 触っているファイル・周辺で見つけた、今の作業と無関係な不具合
- 動いてはいるが明らかに誤っている実装、壊れているテスト、失敗する既存の CI ジョブ
- コードと乖離した Wiki 記述(Wiki 側は直す。乖離の原因がコードの不具合ならそれを起票する)
- 修正すると PR が肥大化する / レビュー単位が混ざるため切り出したい変更

対象外 — 起票しないもの:

- 今の作業の一部として直したもの(PR の説明に書けば足りる)
- 数行で直せて今の変更に自然に含まれるもの
- 「気になる」程度の主観的な指摘、確証のない推測

手順:

```bash
gh issue create --label bug --title "<一文で症状>" --body "<本文>"
# Projects を使う場合のみ:
# gh project item-add <番号> --owner <owner> --url <作成された issue の URL>
```

本文には次を書く。**推測でなく、実際に確認した事実を書く。**

- 発見した経緯: どの作業中に(Issue / PR 番号)、どのファイルで(`path/to/file.py:123`)
- 症状と再現手順、期待する挙動
- 今の PR で直さない理由(一行)

起票したら、その作業の完了報告で Issue 番号を挙げる(例: 「作業中に別件を 1 件発見、#45 で起票」)。
同じ問題の open Issue が既にあれば重複起票せず、その Issue に新事実をコメントで追記する。

### 粒度 — 何件に分けるか

**基本単位は「1 PR で閉じられる修正単位」。** 指摘の数と Issue の数は一致しない。
複数の指摘が同じ修正で片付くなら 1 件にまとめ、別々の修正になるなら分ける。

判断材料。**1 つでも「いいえ」があるなら分ける。**

- **直す場所は同じか。** 同じファイルの同じ節・同じ関数への編集に収まるか。
- **まとめて直す必要があるか。** 片方だけ直しても意味があるなら、それは独立した Issue。
- **根本原因は同じか。** 症状が似ていても原因が別なら別件(対処が別になるため)。
- **タイトルが一文で書けるか。** 「〜と〜」の並列になるなら分割のサイン。

**粗すぎる検知 — クローズ条件が一文で書けないなら粗い。**
「CLAUDE.md を見直す」「エラー処理を改善する」は着手点が無く、いつ閉じてよいかも決まらない。

**細かすぎる検知 — その 1 件のために PR を作るのが割に合わないなら細かい。**
誤字・節番号のような些細な指摘は、関連する Issue に相乗りさせるか、
上記「対象外」に倒して起票しない。

既存の open Issue と範囲が重なるなら、粒度を考える前にそちらへコメントで追記する。
範囲が明らかに広いなら、既存 Issue にコメントした上で別途起票し、相互に番号で参照させる。

### 共通の注意

- private リポジトリでも Webhook URL・トークン・鍵などの秘匿情報を Issue 本文に書かない。
- この運用ルールの範囲(Issue 作成・クローズ・Projects 追加・ステータス更新)は
  事前承認済みの定型作業として都度確認不要。ただし範囲外の Issue 操作
  (手動起票のバグ報告のクローズなど)は従来通り確認を取る。

## 完了の定義 (Definition of Done)

- 変更に関係するテストをローカルで実行し、パスを確認している。
- CI（`gh pr checks`）が全件パスしている。
- スキーマ変更がある場合、マイグレーションが含まれている。
- フロント変更は実装後にブラウザで動作検証している。
- **`wiki/` の該当ページと `wiki/log.md` を同じ PR で更新している**
  (更新不要と判断した場合は PR 本文にその理由を一行書いている)。
- Wiki を更新した場合、**コミット後に `node scripts/wiki-lint.js` が通っている**
  (`updated` の判定はコミット後の状態を見るため、コミット前に走らせても意味がない)。
- **作業中に見つけてこの PR で直さないと決めた問題を、すべて Issue 化している**
  (「スコープ外の問題を発見したとき」を参照。1 件も無ければ、それ自体を確認済みとする。
  Issue 連携を採用しない場合はこの行を削除する)。
- 変更内容の要約と手動確認結果を報告している。起票した Issue があれば番号を挙げている。
- **agy レビューを採用している場合、PR の最新 head が agy レビューを通っている**
  (APPROVE、または差し戻しを評価して誤検知を起票済み)。結果を PR 本文の「レビュー」節に
  書いている。採用しない場合はこの行を削除する。

## [オプション] agy レビュー

PR の CI が緑になった後、マージの前に、差分を agy (Antigravity CLI) 経由で Gemini に
1 回レビューさせる仕組み。採用しない場合は `review/README.md` 冒頭の一覧をまとめて削除する。

手順は `agy-review` スキル、仕組みと設計判断は `review/README.md` にある。
ここに書くのは、スキルが発火しなかった場合でも守られている必要がある不変条件だけ。

- **レビューは必須。CI 緑の後、マージの前に行う。** 順序は
  push → CI → `python review/agy_review.py` → (差し戻しなら評価)→ マージ。
  レビュー記録の無いマージは `gh pr merge` の hook が差し止める。回避手段は置いていない。
- **`gh pr merge` には必ず PR 番号を付ける**(`gh pr merge <PR番号> --merge`)。
  hook は番号で PR を特定するため、番号の無いマージは差し止められる。
- **レビューは head SHA ごとに有効。** 修正を push したら CI から回し直す。
- **差し戻しは 1 件ずつ評価する。** 妥当な指摘が 1 件でもあれば、直す場所まで戻って修正する。
  誤検知と判定するなら成立しない理由をコードの場所つきで書き、
  **`kant0123/gemini-review` に `false-positive` で起票してから** `--triage` で評価を記録してマージする。
  妥当だがスコープ外の指摘は、このリポジトリに起票する。
- **レビューが失敗(agy のエラー・空出力)したままマージしない。**
- **レポートをコミットしない。** `review/work/` は生成物で `.gitignore` 対象。
  残す価値のある知見は `wiki/` に還元する(レポートそのものを貼らない)。

## プロジェクト固有ルール [記入]

- テストコマンド: `<例: python -m pytest tests/ -v / npm test / go test ./...>`
- 命名規約: `<例: DB カラムは大文字スネークケース、Python は snake_case>`
- コミットメッセージの言語: `<例: 日本語>`
- 外部サービス送信のエラー方針: `<例: ベストエフォート、失敗で本体処理を止めない>`
- 秘匿情報の扱い: `<例: Webhook URL・トークン・鍵はログ出力・コミット禁止>`
